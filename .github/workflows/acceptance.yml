name: Real server acceptance
on:
  push:
  pull_request:
  workflow_dispatch:
    inputs:
      seconds:
        description: Sustained WSS messaging duration
        type: choice
        options: ['120', '600']
        default: '600'
permissions:
  contents: read
jobs:
  interoperability:
    runs-on: ubuntu-latest
    timeout-minutes: 25
    steps:
      - uses: actions/checkout@9c091bb21b7c1c1d1991bb908d89e4e9dddfe3e0 # v7.0.0
        with:
          persist-credentials: false
      - uses: actions/checkout@9c091bb21b7c1c1d1991bb908d89e4e9dddfe3e0 # v7.0.0
        with:
          repository: WuKongIM/WuKongIM
          ref: 27a39f15bf163b433f417b78ab6bfc6e589585e5
          path: test-server
          persist-credentials: false
      - uses: actions/setup-go@924ae3a1cded613372ab5595356fb5720e22ba16 # v6.5.0
        with:
          go-version: '1.25.11'
          cache-dependency-path: test-server/go.sum
      - uses: actions/setup-node@249970729cb0ef3589644e2896645e5dc5ba9c38 # v6.5.0
        with:
          node-version: '22.12.0'
      - name: Install Rust
        run: rustup toolchain install 1.86.0 --profile minimal && rustup default 1.86.0
      - name: Verify TLS and protocol contracts
        run: cargo test --locked
      - name: Verify WSS messaging, three interruptions and cleanup
        env:
          TEST_SECONDS: ${{ inputs.seconds || '120' }}
        run: python3 tests/acceptance/run.py --server-source test-server --seconds "$TEST_SECONDS"
      - name: Retain successful acceptance receipt
        uses: actions/upload-artifact@043fb46d1a93c77aae656e7c1c64a875d1fc6a0a # v7.0.1
        with:
          name: rust-sdk-acceptance-${{ github.sha }}
          path: .acceptance/receipt.json
          if-no-files-found: error
          retention-days: 30
