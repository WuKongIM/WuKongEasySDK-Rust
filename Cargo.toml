[package]
name = "wukong-easy-sdk"
version = "0.1.0"
edition = "2021"
rust-version = "1.86"
description = "Lightweight async Rust SDK for WuKongIM WebSocket JSON-RPC messaging"
license = "ISC"
repository = "https://github.com/WuKongIM/WuKongEasySDK-Rust"
readme = "README.md"
keywords = ["wukongim", "im", "websocket", "sdk", "async"]
categories = ["network-programming", "asynchronous"]
include = ["src/**", "examples/**", "tests/*.rs", "tests/interop.mjs", "Cargo.toml", "Cargo.lock", "README*", "LICENSE", "CHANGELOG.md", "docs/**"]

[dependencies]
base64 = "0.22.1"
futures-util = { version = "0.3.31", features = ["sink"] }
serde = { version = "1.0.219", features = ["derive"] }
serde_json = "1.0.140"
rustls = { version = "0.23.44", default-features = false, features = ["ring", "std", "tls12"] }
webpki-roots = "1.0.9"
thiserror = "2.0.12"
tokio = { version = "1.44.2", features = ["net", "sync", "time", "rt", "macros"] }
tokio-tungstenite = { version = "0.28.0", features = ["rustls-tls-webpki-roots"] }
tokio-util = { version = "0.7.14", features = ["rt"] }
uuid = { version = "1.16.0", features = ["v4"] }

[dev-dependencies]
tokio = { version = "1.44.2", features = ["rt-multi-thread", "test-util", "io-util", "io-std", "signal"] }

rcgen = "0.13.2"
tokio-rustls = { version = "0.26.5", default-features = false, features = ["ring", "tls12"] }
# rcgen's date helper must remain buildable on the SDK's Rust 1.86 MSRV.
time = "=0.3.44"
